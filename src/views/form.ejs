<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <div class="container">
        <h1 style="text-align: center; margin-bottom: 30px;"><%= title %></h1>
        
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message" style="max-width: 600px; margin: 0 auto 20px;">
                <%= error %>
            </div>
        <% } %>
        
        <form method="POST" action="/new">
            <label for="messageUser">Your Name:</label>
            <input type="text" id="messageUser" name="messageUser" required placeholder="Enter your name">
            
            <label for="messageText">Message:</label>
            <textarea id="messageText" name="messageText" required placeholder="What's on your mind?"></textarea>
            
            <button type="submit">Submit Message</button>
        </form>
        
        <div style="text-align: center;">
            <a href="/" class="back-link">← Back to Messages</a>
        </div>
    </div>
</body>
</html>