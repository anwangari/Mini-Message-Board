<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <h1><%= title %></h1>
    <div class="message-detail">
        <div class="detail-row">
            <div class="detail-label">From</div>
            <div class="detail-value"><%= message.user %></div>
        </div>
        
        <div class="detail-row">
            <div class="detail-label">Message</div>
            <div class="message-text-detail"><%= message.text %></div>
        </div>
        
        <div class="detail-row">
            <div class="detail-label">Posted On</div>
            <div class="detail-value"><%= message.added.toLocaleString() %></div>
        </div>
    </div>
    <a href="/" class="back-link">← Back to All Messages</a>
</body>
</html>